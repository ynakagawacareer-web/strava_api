# Strava × Claude アクティビティ分析アプリ 引き継ぎ書

> Cowork での開発引き継ぎ用ドキュメント  
> 作成日：2026年6月18日

---

## 1. このプロジェクトの目的

GarminでバドミントンとランニングのアクティビティをStravaに同期し、Strava APIで自動取得、Claudeがコメント・分析を返すWebアプリを作る。スクリーンショット貼り付けなどの手作業を極力なくすのが目標。

---

## 2. 全体アーキテクチャ

```
Garmin デバイス
　↓（自動同期）
Strava
　↓（Strava API / OAuth 2.0）
Web アプリ（HTML + JS）
　↓（Claude API）
分析コメント表示
```

---

## 3. 技術スタック

| 役割 | 使用技術 | 備考 |
|---|---|---|
| アクティビティ記録 | Garmin → Strava 自動同期 | 設定済み前提 |
| データ取得 | Strava API（REST） | 個人利用・無料 |
| フロントエンド | HTML / CSS / JavaScript | シングルファイル |
| AI 分析 | Claude API（claude-sonnet-4-6） | Artifact の Claude in Claude 機能 |
| ホスティング | GitHub Pages | 無料 |
| バージョン管理 | GitHub Desktop | ターミナル不要 |

---

## 4. 取得するデータの設計

### ランニング（GPS あり・フルデータ取得）
- 距離・タイム・ペース
- 心拍数（平均・最大）
- 標高・ルート
- ラップデータ
- 消費カロリー・運動負荷

### バドミントン（屋内・GPSなし）
- 心拍数（平均・最大）
- アクティビティ時間
- 消費カロリー
- ＋ **手入力メモ（1〜2行）**：試合内容・疲労感・気づきなど

---

## 5. 開発ステップ（推奨順序）

### Step 1：Strava API アプリ登録（事前準備）
1. [strava.com/settings/api](https://www.strava.com/settings/api) にアクセス
2. アプリを新規登録（名前は何でも OK）
3. `Client ID` と `Client Secret` を控える
4. `Authorization Callback Domain` に `localhost` を設定（開発中）、後で GitHub Pages の ドメインに変更

### Step 2：OAuth 認証フローの実装
- Strava は OAuth 2.0 を使用
- 初回のみブラウザでログイン・許可が必要
- 取得した `access_token` と `refresh_token` をローカルストレージに保存
- `access_token` は 6 時間で期限切れ → `refresh_token` で自動更新する処理を実装

### Step 3：アクティビティ取得画面の実装
- 最新 N 件のアクティビティ一覧を表示
- ランとバドミントンをアイコンで区別
- バドミントンのみ「メモ入力欄」を表示
- 「分析する」ボタンを設置

### Step 4：Claude API 連携（分析コメント生成）
- Claude API（claude-sonnet-4-6）を呼び出す
- アクティビティ種別に応じてシステムプロンプトを切り替える（下記参照）
- 分析結果をカード形式で表示

### Step 5：GitHub Pages で公開
- GitHub Desktop でコミット → push
- リポジトリ設定 → Pages → main ブランチの /(root) を指定

---

## 6. Claude API のシステムプロンプト設計

### ランニング用
```
あなたはランニングコーチです。以下のアクティビティデータをもとに、
トレーニングの質・疲労状態・次回への改善点を200字程度で簡潔にコメントしてください。

ユーザー情報：
- 目的：バドミントン競技のための体力強化
- トレーニング位置づけ：競技補助（ジム・バドミントンと並行）

以下の観点を重視してください：
- ペースの安定性
- 心拍ゾーンの適切さ
- 疲労蓄積のリスク
- バドミントン大会前後かどうかの文脈（ユーザーが入力した場合）
```

### バドミントン用
```
あなたはバドミントンのフィジカルコーチです。
心拍数データとユーザーのメモをもとに、今日の練習強度・疲労状態・
次回への一言アドバイスを150字程度でコメントしてください。

心拍データからわかること：
- 平均心拍：練習全体の強度
- 最大心拍：瞬間的な高強度場面の有無
- 時間：練習量

ユーザーメモから質的な内容を補完して分析してください。
```

---

## 7. Strava API の主要エンドポイント

```
# 最新アクティビティ一覧（20件）
GET https://www.strava.com/api/v3/athlete/activities?per_page=20

# アクティビティ詳細（ラップ含む）
GET https://www.strava.com/api/v3/activities/{id}

# トークンリフレッシュ
POST https://www.strava.com/oauth/token
  grant_type=refresh_token
  client_id=xxx
  client_secret=xxx
  refresh_token=xxx
```

---

## 8. 注意事項

- **APIキーは HTML に直書きしない**。GitHub に上げると公開されてしまう。ローカルストレージに保存する形にする
- Strava のレート制限：15分あたり200リクエスト、1日2000リクエスト（個人利用なら余裕）
- **Strava データを AI モデルの学習に使うことは規約違反**。あくまで自分の分析用途に限定する
- GitHub Pages は**パブリックリポジトリ**で無料。APIキーをコードに含めないよう徹底する

---

## 9. 将来の拡張アイデア（優先度低）

- [ ] 週次サマリー（7日間のアクティビティをまとめてClaudeが総評）
- [ ] バドミントン大会前後の疲労管理アラート
- [ ] ランのペース推移グラフ表示
- [ ] LOL試合振り返りアプリとダッシュボードを統合

---

## 10. Cowork 起動時に Claude に渡すプロンプト

以下をそのままコピペしてください：

---

> 【引き継ぎ】Strava × Claude のアクティビティ分析Webアプリを作ってください。
>
> **目的**：GarminのランニングとバドミントンのデータをStrava APIで自動取得し、Claudeがコメントを返すアプリ。スクリーンショット貼り付けなどの手作業をなくしたい。
>
> **技術構成**：HTML シングルファイル / Strava API（OAuth 2.0）/ Claude API（claude-sonnet-4-6）/ GitHub Pages
>
> **データ設計**：
> - ランニング → Strava APIからフル自動取得（距離・ペース・心拍など）
> - バドミントン → 心拍・時間を自動取得 ＋ 一言メモを手入力
>
> **開発順序**：
> 1. Strava OAuth 認証フロー（access_token 取得・refresh_token 自動更新）
> 2. アクティビティ一覧表示（ラン・バドを区別、バドにはメモ欄）
> 3. 「分析する」ボタン → Claude API 呼び出し → コメント表示
> 4. GitHub Pages で公開
>
> APIキーはコードに直書きせず、ローカルストレージに保存する形にしてください。
> まずStep 1のOAuth認証フローから実装を始めてください。

---

*以上*
